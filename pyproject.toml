[project]
name = "flet-app"
version = "0.1.0"
description = "LG webOS remote control with a mobile-first Flet UI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flet[all]>=0.86.5",
    "requests",
    "websockets>=15.0",
    "python-dotenv>=1.0",
]
